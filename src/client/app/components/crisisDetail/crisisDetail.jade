div(ng-if="$ctrl.crisis")
	h3 "{{ $ctrl.editName }}"
	div
		label Id: {{ $ctrl.crisis.id }}
	div
		label Name:
		input(ng-model="$ctrl.editName" placeholder="name")
	button(ng-click="$ctrl.save()") Save
	button(ng-click="$ctrl.cancel()") Cancel